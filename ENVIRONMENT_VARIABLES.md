# 🔐 متغيرات البيئة - Environment Variables Documentation

## نظام إدارة المعلومات الاستخباراتية - IIMS System

هذا الملف يوثق جميع متغيرات البيئة المستخدمة في نظام IIMS.

---

## 📋 جدول المحتويات

1. [المتغيرات الإلزامية](#المتغيرات-الإلزامية)
2. [متغيرات قاعدة البيانات](#متغيرات-قاعدة-البيانات)
3. [متغيرات المستخدم الإداري](#متغيرات-المستخدم-الإداري)
4. [متغيرات الأمان](#متغيرات-الأمان)
5. [متغيرات Groq AI](#متغيرات-groq-ai)
6. [متغيرات الأداء](#متغيرات-الأداء)
7. [متغيرات المنصات](#متغيرات-المنصات)
8. [متغيرات اختيارية](#متغيرات-اختيارية)

---

## المتغيرات الإلزامية

### `SECRET_KEY`

**الوصف:** مفتاح Django السري المستخدم للتشفير والتوقيع.

**القيمة الافتراضية:** 
- Development: `django-insecure-7kbmjh&#j#r5525=%60fryv1c9@#%4p9rw_wmyq4n%uyu&%28p`
- Production: **يجب تعيينه**

**كيفية التوليد:**
```bash
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
```

**مثال:**
```env
SECRET_KEY=django-insecure-abc123xyz789!@#$%^&*()
```

**⚠️ تحذير:** لا تستخدم المفتاح الافتراضي في الإنتاج!

---

### `DEBUG`

**الوصف:** تفعيل/تعطيل وضع التطوير.

**القيم المقبولة:** `True`, `False`

**القيمة الافتراضية:** `True` (في التطوير)

**الإنتاج:**
```env
DEBUG=False
```

**التطوير:**
```env
DEBUG=True
```

**⚠️ تحذير:** يجب أن يكون `False` في الإنتاج لأسباب أمنية!

---

### `ALLOWED_HOSTS`

**الوصف:** قائمة النطاقات المسموح لها بالوصول للتطبيق (مفصولة بفواصل).

**القيمة الافتراضية:**
- Development: `127.0.0.1,localhost,0.0.0.0`
- Production: يتم الكشف تلقائياً من متغيرات المنصة

**مثال:**
```env
ALLOWED_HOSTS=myapp.railway.app,www.myapp.com,myapp.com
```

**ملاحظة:** لا تستخدم `*` في الإنتاج!

---

## متغيرات قاعدة البيانات

### `DATABASE_URL`

**الوصف:** رابط الاتصال بقاعدة البيانات (PostgreSQL موصى به).

**التنسيق:**
```
postgresql://username:password@host:port/database
```

**أمثلة:**

**Railway (تلقائي):**
```env
DATABASE_URL=postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway
```

**Render (تلقائي):**
```env
DATABASE_URL=postgresql://user:pass@dpg-xxx.oregon-postgres.render.com/dbname
```

**محلي:**
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/iims_db
```

**SQLite (تطوير فقط):**
```env
# لا حاجة لتعيين DATABASE_URL
# سيستخدم Django ملف db.sqlite3 تلقائياً
```

---

## متغيرات المستخدم الإداري

هذه المتغيرات تستخدم بواسطة أمر `ensure_admin` لإنشاء/تحديث المستخدم الإداري.

### `ADMIN_USERNAME`

**الوصف:** اسم المستخدم الإداري.

**القيمة الافتراضية:** `admin`

**مثال:**
```env
ADMIN_USERNAME=admin
```

---

### `ADMIN_EMAIL`

**الوصف:** البريد الإلكتروني للمستخدم الإداري.

**القيمة الافتراضية:** `admin@iims.local`

**مثال:**
```env
ADMIN_EMAIL=admin@example.com
```

---

### `ADMIN_PASSWORD`

**الوصف:** كلمة مرور المستخدم الإداري.

**القيمة الافتراضية:** لا يوجد (يجب تعيينه)

**مثال:**
```env
ADMIN_PASSWORD=SecurePassword123!@#
```

**⚠️ مهم جداً:** استخدم كلمة مرور قوية!

**متطلبات كلمة المرور:**
- 8 أحرف على الأقل
- تحتوي على أحرف كبيرة وصغيرة
- تحتوي على أرقام
- تحتوي على رموز خاصة

---

### `ADMIN_JOB_NUMBER`

**الوصف:** الرقم الوظيفي للمستخدم الإداري.

**القيمة الافتراضية:** `ADMIN-001`

**مثال:**
```env
ADMIN_JOB_NUMBER=ADMIN-001
```

---

### `ADMIN_RANK`

**الوصف:** الرتبة العسكرية للمستخدم الإداري.

**القيمة الافتراضية:** `MIL`

**القيم المقبولة:** `MIL`, `CIV`, `OFFICER`, إلخ.

**مثال:**
```env
ADMIN_RANK=MIL
```

---

### `ADMIN_FIRST_NAME`

**الوصف:** الاسم الأول للمستخدم الإداري.

**القيمة الافتراضية:** `System`

**مثال:**
```env
ADMIN_FIRST_NAME=محمد
```

---

### `ADMIN_LAST_NAME`

**الوصف:** اسم العائلة للمستخدم الإداري.

**القيمة الافتراضية:** `Administrator`

**مثال:**
```env
ADMIN_LAST_NAME=الإداري
```

---

## متغيرات الأمان

### `CSRF_TRUSTED_ORIGINS`

**الوصف:** قائمة النطاقات الموثوقة لـ CSRF (مفصولة بفواصل، يجب أن تبدأ بـ https://).

**القيمة الافتراضية:** يتم الكشف تلقائياً من متغيرات المنصة

**مثال:**
```env
CSRF_TRUSTED_ORIGINS=https://myapp.railway.app,https://www.myapp.com
```

---

### `SECURE_SSL_REDIRECT`

**الوصف:** إعادة توجيه HTTP إلى HTTPS تلقائياً.

**القيمة الافتراضية:** `True` (في الإنتاج)

**مثال:**
```env
SECURE_SSL_REDIRECT=True
```

---

### `SECURE_HSTS_SECONDS`

**الوصف:** مدة HSTS بالثواني.

**القيمة الافتراضية:** `31536000` (سنة واحدة)

**مثال:**
```env
SECURE_HSTS_SECONDS=31536000
```

---

### `SECURE_HSTS_INCLUDE_SUBDOMAINS`

**الوصف:** تطبيق HSTS على النطاقات الفرعية.

**القيمة الافتراضية:** `True`

**مثال:**
```env
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
```

---

### `SECURE_HSTS_PRELOAD`

**الوصف:** تفعيل HSTS Preload.

**القيمة الافتراضية:** `True`

**مثال:**
```env
SECURE_HSTS_PRELOAD=True
```

---

## متغيرات Groq AI

### `GROQ_API_KEY`

**الوصف:** مفتاح API لخدمة Groq AI (للترجمة والتحليل الذكي).

**القيمة الافتراضية:** لا يوجد (اختياري)

**كيفية الحصول عليه:**
1. انتقل إلى [console.groq.com](https://console.groq.com)
2. سجل حساب جديد
3. انتقل إلى API Keys
4. أنشئ مفتاح جديد

**مثال:**
```env
GROQ_API_KEY=gsk_abc123xyz789...
```

**ملاحظة:** إذا لم يتم تعيينه، ستعمل ميزات AI في وضع محدود.

---

### `GROQ_MODEL`

**الوصف:** نموذج Groq المستخدم للذكاء الاصطناعي.

**القيمة الافتراضية:** `openai/gpt-oss-120b`

**النماذج المتاحة:**
- `openai/gpt-oss-120b` (موصى به - Ollama 3.0)
- `llama-3.3-70b-versatile`
- `mixtral-8x7b-32768`
- `gemma2-9b-it`

**مثال:**
```env
GROQ_MODEL=openai/gpt-oss-120b
```

**ملاحظة:** النظام الحالي يستخدم `openai/gpt-oss-120b` (Ollama 3.0)

---

### `GROQ_REASONING_EFFORT`

**الوصف:** مستوى جهد التفكير للنموذج.

**القيمة الافتراضية:** `medium`

**القيم المقبولة:** `low`, `medium`, `high`

**مثال:**
```env
GROQ_REASONING_EFFORT=medium
```

---

### `GROQ_MAX_COMPLETION_TOKENS`

**الوصف:** الحد الأقصى لعدد الرموز في الاستجابة.

**القيمة الافتراضية:** `8192`

**مثال:**
```env
GROQ_MAX_COMPLETION_TOKENS=8192
```

---

### `GROQ_TEMPERATURE`

**الوصف:** درجة حرارة النموذج (الإبداع).

**القيمة الافتراضية:** `0.3`

**النطاق:** `0.0` - `2.0`
- `0.0` - `0.3`: دقيق ومحدد
- `0.4` - `0.7`: متوازن
- `0.8` - `2.0`: إبداعي

**مثال:**
```env
GROQ_TEMPERATURE=0.3
```

---

### `REQUIRE_GROQ_API_KEY`

**الوصف:** إجبار وجود مفتاح Groq (يوقف التطبيق إذا لم يكن موجوداً).

**القيمة الافتراضية:** `False`

**مثال:**
```env
REQUIRE_GROQ_API_KEY=False
```

---

## متغيرات الأداء

### `WEB_CONCURRENCY`

**الوصف:** عدد Gunicorn workers.

**القيمة الافتراضية:** `3`

**الحساب الموصى به:** `(2 × CPU cores) + 1`

**مثال:**
```env
WEB_CONCURRENCY=3
```

---

### `PORT`

**الوصف:** منفذ التطبيق.

**القيمة الافتراضية:** `8004`

**مثال:**
```env
PORT=8004
```

**ملاحظة:** Railway وRender يعينون هذا تلقائياً.

---

### `DB_CONN_MAX_AGE`

**الوصف:** مدة الاحتفاظ باتصال قاعدة البيانات (بالثواني).

**القيمة الافتراضية:** `600` (10 دقائق)

**مثال:**
```env
DB_CONN_MAX_AGE=600
```

---

## متغيرات المنصات

### Railway

**يتم توفيرها تلقائياً:**

```env
RAILWAY_PUBLIC_DOMAIN=myapp.railway.app
RAILWAY_PRIVATE_DOMAIN=myapp.railway.internal
RAILWAY_STATIC_URL=https://...
DATABASE_URL=postgresql://...
PORT=8004
```

---

### Render

**يتم توفيرها تلقائياً:**

```env
RENDER_EXTERNAL_URL=https://myapp.onrender.com
DATABASE_URL=postgresql://...
PORT=10000
```

---

### Vercel

**يتم توفيرها تلقائياً:**

```env
VERCEL_URL=myapp.vercel.app
VERCEL_ENV=production
```

---

## متغيرات اختيارية

### `LANGUAGE_CODE`

**الوصف:** لغة التطبيق.

**القيمة الافتراضية:** `ar` (العربية)

**مثال:**
```env
LANGUAGE_CODE=ar
```

---

### `TIME_ZONE`

**الوصف:** المنطقة الزمنية.

**القيمة الافتراضية:** `Asia/Riyadh`

**مثال:**
```env
TIME_ZONE=Asia/Riyadh
```

---

### `LOG_LEVEL`

**الوصف:** مستوى السجلات.

**القيمة الافتراضية:** `INFO`

**القيم المقبولة:** `DEBUG`, `INFO`, `WARNING`, `ERROR`, `CRITICAL`

**مثال:**
```env
LOG_LEVEL=INFO
```

---

## 📝 قوالب سريعة

### Railway Production

```env
# Core
SECRET_KEY=your-generated-secret-key
DEBUG=False
ALLOWED_HOSTS=myapp.railway.app

# Database (provided by Railway)
# DATABASE_URL=postgresql://...

# Admin
ADMIN_USERNAME=ai-iimms
ADMIN_EMAIL=admin@iims.local
ADMIN_PASSWORD=SecurePassword123!
ADMIN_JOB_NUMBER=ADMIN-001

# Groq AI (required for AI features)
GROQ_API_KEY=gsk_your_actual_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
GROQ_REASONING_EFFORT=medium
GROQ_MAX_COMPLETION_TOKENS=8192
GROQ_TEMPERATURE=1
```

**ملاحظة:** احصل على مفتاح Groq API من https://console.groq.com

---

### Render Production

```env
# Core
SECRET_KEY=your-generated-secret-key
DEBUG=False
PYTHON_VERSION=3.11.0

# Database (provided by Render)
# DATABASE_URL=postgresql://...

# Admin
ADMIN_PASSWORD=SecurePassword123!

# Performance
WEB_CONCURRENCY=2
```

---

### Local Development

```env
# Core
SECRET_KEY=django-insecure-local-dev-key
DEBUG=True

# Database (SQLite)
# No DATABASE_URL needed

# Admin
ADMIN_PASSWORD=admin123

# Groq AI (optional)
GROQ_API_KEY=gsk_your_key_here
```

---

## 🔒 أفضل الممارسات الأمنية

1. **لا تشارك المفاتيح السرية:**
   - لا تضع `.env` في Git
   - استخدم `.env.example` للتوثيق فقط

2. **استخدم كلمات مرور قوية:**
   - 12 حرف على الأقل
   - مزيج من الأحرف والأرقام والرموز

3. **قم بتدوير المفاتيح بانتظام:**
   - غيّر SECRET_KEY كل 6 أشهر
   - غيّر ADMIN_PASSWORD بانتظام

4. **استخدم متغيرات البيئة:**
   - لا تضع المفاتيح في الكود
   - استخدم أدوات إدارة الأسرار

5. **راقب الوصول:**
   - فعّل السجلات
   - راجع محاولات تسجيل الدخول

---

## 📞 المساعدة

للمزيد من المعلومات:
- راجع [DEPLOYMENT.md](DEPLOYMENT.md)
- راجع [README.md](README.md)
- راجع `.env.production.example`

---

**آخر تحديث:** 2024
