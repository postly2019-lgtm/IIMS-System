# نظام الوكيل الذكي (AI Agent Service)

خدمة مصغرة (Microservice) لمعالجة طلبات الذكاء الاصطناعي باستخدام Groq API.

## الميزات
- ⚡ **سريع جداً**: يعتمد على Groq LPU.
- 🔄 **دعم البث (Streaming)**: استجابة فورية.
- 🛡️ **مفصول**: يعمل بشكل مستقل عن النظام الرئيسي.
- 📋 **متوافق**: واجهة برمجة تطبيقات مشابهة لـ OpenAI.

## الإعداد والتشغيل المحلي

1. **تثبيت التبعيات**:
   ```bash
   pip install -r requirements.txt
   ```

2. **إعداد المتغيرات البيئية**:
   انسخ ملف `.env.example` إلى `.env` وقم بتعبئة القيم:
   ```bash
   cp .env.example .env
   ```

3. **تشغيل الخدمة**:
   ```bash
   python app.py
   ```

## النشر (Deployment)

### Railway
1. أنشئ مستودع GitHub جديد وارفع هذه الملفات.
2. اربط المستودع بـ Railway.
3. أضف المتغيرات البيئية في لوحة تحكم Railway:
   - `GROQ_API_KEY`
   - `GROQ_MODEL`

## نقاط النهاية (Endpoints)
- `GET /health`: فحص حالة النظام.
- `POST /api/v1/chat`: إرسال رسائل والحصول على ردود.
- `GET /api/v1/models`: عرض النماذج المتاحة.
