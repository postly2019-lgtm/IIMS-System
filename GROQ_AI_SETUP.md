# 🤖 إعداد Groq AI - Groq AI Configuration

## نظام IIMS - تكوين الذكاء الاصطناعي

---

## 📋 معلومات النموذج الحالي

### النموذج المستخدم: Ollama 3.0

**اسم النموذج في Groq:** `openai/gpt-oss-120b`

**المواصفات:**
- **الحجم:** 120 مليار معامل
- **السياق:** 8192 token
- **اللغات:** متعدد اللغات (بما في ذلك العربية)
- **الاستخدام:** ترجمة، تحليل، استخراج معلومات

---

## 🔑 معلومات الحساب

### بيانات الاعتماد

```env
ADMIN_USERNAME=ai-iimms
GROQ_API_KEY=gsk_your_actual_groq_api_key_here
```

**⚠️ تحذير أمني:**
- هذا المفتاح حساس جداً
- لا تشاركه مع أحد
- لا تضعه في Git
- استخدمه فقط في متغيرات البيئة
- احصل على مفتاحك من: https://console.groq.com

---

## ⚙️ الإعدادات الموصى بها

### للإنتاج (Production)

```env
# Groq AI Configuration
GROQ_API_KEY=gsk_your_actual_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
GROQ_REASONING_EFFORT=medium
GROQ_MAX_COMPLETION_TOKENS=8192
GROQ_TEMPERATURE=1
```

### للتطوير (Development)

```env
# Groq AI Configuration
GROQ_API_KEY=gsk_your_actual_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
GROQ_REASONING_EFFORT=low
GROQ_MAX_COMPLETION_TOKENS=4096
GROQ_TEMPERATURE=0.7
```

---

## 📝 شرح المعاملات

### `GROQ_MODEL`

**القيمة:** `openai/gpt-oss-120b`

**الوصف:** نموذج Ollama 3.0 من Groq

**البدائل المتاحة:**
- `llama-3.3-70b-versatile` - Llama 3.3 (70B)
- `mixtral-8x7b-32768` - Mixtral 8x7B
- `gemma2-9b-it` - Gemma 2 (9B)

---

### `GROQ_REASONING_EFFORT`

**القيمة:** `medium`

**الوصف:** مستوى جهد التفكير

**الخيارات:**
- `low` - سريع، أقل دقة
- `medium` - متوازن (موصى به)
- `high` - بطيء، أكثر دقة

---

### `GROQ_MAX_COMPLETION_TOKENS`

**القيمة:** `8192`

**الوصف:** الحد الأقصى لطول الاستجابة

**النطاق:** `1` - `8192`

**التوصيات:**
- ترجمة قصيرة: `2048`
- ترجمة متوسطة: `4096`
- ترجمة طويلة: `8192`

---

### `GROQ_TEMPERATURE`

**القيمة:** `1`

**الوصف:** درجة الإبداع/العشوائية

**النطاق:** `0.0` - `2.0`

**التوصيات:**
- ترجمة دقيقة: `0.3` - `0.5`
- ترجمة طبيعية: `0.7` - `1.0` (الحالي)
- إبداعية: `1.2` - `2.0`

---

## 🔧 كود الاستخدام

### مثال Python

```python
from groq import Groq
import os

# استخدم المفتاح من متغيرات البيئة
client = Groq(api_key=os.getenv("GROQ_API_KEY"))

completion = client.chat.completions.create(
    model="openai/gpt-oss-120b",
    messages=[
        {
            "role": "user",
            "content": "ترجم هذا النص إلى الإنجليزية: مرحباً بك في نظام IIMS"
        }
    ],
    temperature=1,
    max_completion_tokens=8192,
    reasoning_effort="medium",
    stream=True,
    stop=None
)

for chunk in completion:
    print(chunk.choices[0].delta.content or "", end="")
```

---

## 🚀 التكامل مع IIMS

### في `intelligence_agent/services.py`

النظام يستخدم Groq AI في:

1. **الترجمة التلقائية:**
   - ترجمة التقارير من العربية إلى الإنجليزية
   - ترجمة المحتوى للتحليل

2. **التحليل الذكي:**
   - تحليل التقارير الاستخباراتية
   - استخراج المعلومات الرئيسية
   - تصنيف المحتوى

3. **البحث الدلالي:**
   - البحث في قاعدة المعرفة
   - الإجابة على الأسئلة
   - توليد الملخصات

---

## 📊 حدود الاستخدام

### Groq API Limits

**الخطة المجانية:**
- 14,400 requests/day
- 30 requests/minute
- 6,000 tokens/minute

**الخطة المدفوعة:**
- حدود أعلى
- أولوية في المعالجة
- دعم فني

**مراقبة الاستخدام:**
```bash
# في Groq Console
https://console.groq.com/usage
```

---

## 🔍 اختبار الاتصال

### اختبار سريع

```bash
curl https://api.groq.com/openai/v1/models \
  -H "Authorization: Bearer $GROQ_API_KEY"
```

**الاستجابة المتوقعة:**
```json
{
  "object": "list",
  "data": [
    {
      "id": "openai/gpt-oss-120b",
      "object": "model",
      ...
    }
  ]
}
```

### اختبار من Django

```bash
python manage.py shell
```

```python
from intelligence_agent.services import GroqClient

client = GroqClient()
response = client.chat("مرحباً، كيف حالك؟")
print(response)
```

---

## ⚠️ استكشاف الأخطاء

### خطأ: Invalid API Key

**الرسالة:**
```
Error code: 401 - Invalid API key
```

**الحل:**
1. تحقق من `GROQ_API_KEY` في متغيرات البيئة
2. تأكد من عدم وجود مسافات إضافية
3. تحقق من صلاحية المفتاح في Groq Console

---

### خطأ: Model Not Found

**الرسالة:**
```
Model 'openai/gpt-oss-120b' not found
```

**الحل:**
1. تحقق من اسم النموذج
2. تأكد من توفر النموذج في حسابك
3. جرب نموذج بديل مؤقتاً

---

### خطأ: Rate Limit Exceeded

**الرسالة:**
```
Rate limit exceeded
```

**الحل:**
1. انتظر دقيقة وحاول مرة أخرى
2. قلل عدد الطلبات
3. ترقية إلى خطة مدفوعة

---

## 🔐 أفضل الممارسات الأمنية

### 1. حماية المفتاح

```bash
# ✅ صحيح - في متغيرات البيئة
export GROQ_API_KEY="gsk_..."

# ❌ خطأ - في الكود
GROQ_API_KEY = "gsk_..."  # لا تفعل هذا!
```

### 2. تدوير المفاتيح

- غيّر المفتاح كل 3-6 أشهر
- غيّره فوراً إذا تم تسريبه
- احتفظ بنسخة احتياطية آمنة

### 3. مراقبة الاستخدام

- راقب الاستخدام اليومي
- فعّل التنبيهات للاستخدام الزائد
- راجع السجلات بانتظام

---

## 📞 الدعم

### موارد Groq

- **Console:** https://console.groq.com
- **Docs:** https://console.groq.com/docs
- **Discord:** https://discord.gg/groq
- **Status:** https://status.groq.com

### موارد IIMS

- **DEPLOYMENT.md** - دليل النشر
- **ENVIRONMENT_VARIABLES.md** - توثيق المتغيرات
- **README.md** - نظرة عامة

---

## 📝 ملاحظات إضافية

### للنشر على Railway

```env
# في Railway Dashboard → Variables
GROQ_API_KEY=gsk_your_actual_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
GROQ_TEMPERATURE=1
GROQ_REASONING_EFFORT=medium
GROQ_MAX_COMPLETION_TOKENS=8192
```

### للتطوير المحلي

```bash
# في ملف .env (لا تضعه في Git!)
GROQ_API_KEY=gsk_your_actual_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
```

---

**آخر تحديث:** 2024

**النموذج الحالي:** Ollama 3.0 (openai/gpt-oss-120b)

**الحساب:** ai-iimms
