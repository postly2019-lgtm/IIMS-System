# إعدادات النظام الأساسية
# يرجى نسخ هذا الملف إلى .env وتعبئة القيم

# المفتاح السري (يجب تغييره في البيئة الإنتاجية)
SECRET_KEY=django-insecure-change-me-in-production-!@#$

# وضع التصحيح (True للتطوير، False للإنتاج)
DEBUG=True

# مفتاح API الخاص بـ Groq (للذكاء الاصطناعي)
# احصل عليه من: https://console.groq.com
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxx

# إعدادات قاعدة البيانات (اختياري - الافتراضي SQLite)
# DATABASE_URL=postgres://user:password@localhost:5432/dbnam
# ⚠️ ⚠️ ⚠️ تحذير: لا ترفع هذا الملف إلى GitHub ⚠️ ⚠️ ⚠️
GROQ_MODEL=llama-3.3-70b-versatile
